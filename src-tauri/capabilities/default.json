{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Main window: core IPC + file-open dialog",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "dialog:default"
  ]
}
